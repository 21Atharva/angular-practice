import { ReversPipePipe } from './revers-pipe.pipe';

describe('ReversPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReversPipePipe();
    expect(pipe).toBeTruthy();
  });
});
